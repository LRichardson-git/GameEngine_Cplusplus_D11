#include "SwapChain.h"
#include "GraphicsEngine.h"

SwapChain::SwapChain()
{
}

bool SwapChain::init()
{
	GraphicsEngine::get()->m_

	//in windows already one buffer (desktop itself)

	return true;
}

bool SwapChain::release()
{
	return true;
}

SwapChain::~SwapChain()
{
}
